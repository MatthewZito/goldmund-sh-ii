module.exports = {
  plugins: {
    autoprefixer: {
      grid: 'autoplace'
    },
    'postcss-preset-env': {
      stage: 1
    },
    'postcss-import': {}
  }
};
// router.beforeEach((to, _from, next) => {
//   NProgress.start();
// });

// router.afterEach(() => {
//   NProgress.done();
// });
